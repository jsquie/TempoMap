(
var heap;

heap = MaxMeterHeap();

heap.insert(1, 2);
heap.insert(2, 2);
heap.insert(3, 1);

heap.findMax.postln;
 heap.deleteMax;
 heap.deleteMax;
 heap.deleteMax;
 heap.deleteMax;
 heap.deleteMax;


)
